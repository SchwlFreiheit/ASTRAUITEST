# A.S.T.R.A. UI TEST

Visual and interaction prototype for PROJECT: A.S.T.R.A.

- React + TypeScript + Vite
- GitHub Pages preview
- UI prototype only; no production CORE integration
- No WebGL / Three.js / canvas rendering

The prototype is intentionally structured as normal source files. Do not replace it with a single-file or compressed-loader build.
